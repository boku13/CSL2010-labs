std::ifstream ifs{ file_path };

    if (ifs){

        EntityArray.push_back(Department_Entity);
        EntityArray.push_back(Course_Entity);
        EntityArray.push_back(Hostel_Entity);
        EntityArray.push_back(Club_Entity);

        std::string line;

        while(std::getline(ifs, line)){

            int i = 6;

            std::istringstream DetailsStream(line);



            //pushing an element into the students vector
            std::string Name, Roll_No;
            std::getline(DetailsStream, Name, ',');
            std::getline(DetailsStream, Roll_No, ',');
            StudentRecord studentrecord;
            studentrecord.set_studentName(Name);
            studentrecord.set_rollNumber(Roll_No);
            students.push_back(studentrecord);

            //adding a node in the entity list
            std::string Department;
            Department_Entity.set_name(Department);
            std::getline(DetailsStream, Department, ',');
            Department_Entity.add_student(studentrecord);

            std::string Course;
            Course_Entity.set_name(Course);
            std::getline(DetailsStream, Course, ',');
            Course_Entity.add_student(studentrecord);

            std::string Hostel;
            Hostel_Entity.set_name(Hostel);
            std::getline(DetailsStream, Hostel, ',');
            Hostel_Entity.add_student(studentrecord);

            std::string Club;
            Club_Entity.set_name(Club);
            std::getline(DetailsStream, Club, ',');
            Club_Entity.add_student(studentrecord);
        }
    }
}